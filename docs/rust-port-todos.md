# Rust 移植遗留事项与统一问题索引

合并 2026-09-13 系统审查，保留原 UI 待办全部内容。R01–R56 为首轮审查编号，统一索引见文末；执行依赖和验收标准见 [整体修复方案](rust-refactor-plan.md)。已开始分批修复，实际修改与验证边界见 [修复进展](rust-fix-progress.md)；以下索引保留原问题，不代表全部仍未改动或已经完成。

旧行为参考 Go 的 `internal/ui/automationpanel`、`internal/ui/processpicker`、`internal/ui/nativeform`；准确旧版基线尚待确认。只有实现、必要测试、对应交互验收都通过才关闭项目，不能按代码已写划掉。

## 未解决的 Bug

- **picker/编辑器原生控件偶发"屏幕空白"**：进程列表（ListView）与输入框（EDIT）在部分机器上首次显示不合成到屏幕——数据存在（`LVM_GETITEMCOUNT` 有值、PrintWindow 可渲染），但屏幕像素为空。已加 `present_control`（Go `PresentControl` 等价）缓解，根因未定位。怀疑方向：首次揭示（DWM uncloak）与控件更新区域交互、特定显示器/DPI 组合。复现要点：真实鼠标路径打开，看屏幕（不要用 PrintWindow 截图判断）。

## 行为差距（用户可感知）

1. **进程说明不回传**：确认选择后，编辑器"i"信息对话框与预览卡只显示裸进程名；Go 会在确认时带回 `descriptions` 映射，显示"名称（说明）"（editor.go:493-505、loading_model.go:626-637）。
2. **picker 加载分相位缺失**：Go 有 loading/enriching/error 三相、加载中禁用刷新按钮、扫描失败显示错误文案（loading_model.go:173-247）；当前实现快照失败静默为空列表。另外激活时按快照新旧（15–60 秒）自动刷新（processpicker window_messages.go:508-511）。
3. **管理器选择按索引保持**：Go 跨重填按规则 ID 保持选中（manager.go:135-141）；当前按 `LB_GETCURSEL` 索引，列表顺序变化时会跳选。
4. **保存/启停/删除失败没有事务结果回传**：Go 把保存错误写进管理器状态行或编辑器校验行（editor.go:647-668）。当前磁盘写失败会弹通用警告，但保存接口不返回结果，编辑器仍可能关闭；另有任务序列化错误被吞掉（R01）。失败必须保留草稿与窗口并支持重试。
5. **星期键盘导航**：Go 仅第一个星期按钮有 TabStop，方向键在七个按钮间移动，保存/取消创建顺序为取消在前（editor.go:98-105、controls_layout.go:571-597）。
6. **ListView 列宽不自适应**：Go 按客户区宽度重算三列（含 24px 滚动条预留、190/180 最小值，processpicker.go:903-943）；当前为固定 250/310/82。
7. **描述到达后重置滚动/焦点行**：Go 保存并恢复 top-index 与焦点行（loading_model.go:313-353）；当前全量重建回到顶部。

## 交互细节差距

8. **选择弹窗（choice popup）**：焦点恢复语义（选中后回焦按钮、Escape/F4 还原、焦点在属主内移动不关闭，choicepopup.go:500-544）；按下/释放行配对与按下行视觉（445-461）；超过可视行数时的主题滚动条与每格 1 行滚动（206-223）；从 BN_CLICKED 改为投递消息打开（editor.go:461-469）。
9. **主题切换后复选框位图不重建**：Go 的 `applyTheme` 重建 ListView 状态图（state_images.go）；当前仅创建时生成一次。
10. **编辑器外部变更冲突提示**：Go 的 `pendingState`/`automation_changed_external` 流程（automationpanel.go:578-614）未移植。

## 公共 UI 必须补齐的能力

- 自动化三窗口的 tooltips（Go 为全部字段注册了 tip_automation_*）。
- 内容滚动（编辑器高于视口时的滚动条）与 WM_DPICHANGED 重建（Go nativeform scrollbar/viewport 体系）。
- ListView 原生表头/滚动条的主题化（Go 自绘表头 + 主题滚动条覆盖层）。

## 文档与产物

- Go 时代的 user guide / development / release-notes / roadmap / 截图已随移植删除（git 历史可查）；Rust 版文档按功能达标进度补回。
- `Cargo.lock` 已纳入版本管理（对齐 Go 仓库提交 go.sum 的做法）。

上述 DPI、滚动、字段提示、表头/滚动条主题是保留原交互与视觉的验收条件，不再笼统标为“按需排期”。Cargo.lock 是已完成项。

## 证据边界

前轮使用 devtools 的 GetDC(hwnd)+BitBlt 客户区捕获观察到列表空白；尚未完成真实鼠标路径和完整桌面像素验收。它不是 PrintWindow，状态文字中的条目数也不能替代原生列表条数/插入返回值检查。随后已定位并修复 surface 兄弟层序遮挡，原生窗口测试与深浅色客户区捕获通过，详见修复进展；其他环境的验收仍开放，不能把重复重绘或延时视作已解决。

## 系统审查统一索引

以下全部开放。已验证表示已有独立探针；静态表示源码有错误路径；实机表示仍需对应环境验证。编号按共同原因归包，不表示每项各写一个补丁。

| 编号 | 问题与剩余工作 | 工作包/证据 |
|---|---|---|
| R01 | 任务 TOML 序列化、失败回传与保存后提交 | W2；已验证 |
| R02 | PDH 错误 ABI、缓冲区布局/对齐 | W1；已验证 |
| R03 | DPI 销毁窗口发送退出消息 | W1/W4；静态 |
| R04 | 状态规则聚合及启停/删除撤销 | W3；静态 |
| R05 | 计划进程条件、等待及截止时间 | W3；静态 |
| R06 | 多事件覆盖、动作槽原子占用 | W3；静态 |
| R07 | 自动/手动/暂停/电池统一有效状态 | W3；静态 |
| R08 | 空闲起点、零秒与长告警参数 | W3；静态 |
| R09 | 告警取消、配置变化及执行前校验 | W3；静态 |
| R10 | WinHTTP 主机组件未请求、空指针 | W1；已验证 |
| R11 | 预览不能排入真实重启动作 | W0；静态 |
| R12 | 列表/EDIT 空白，数据与显示双轨诊断 | W4；层序根因已验证，完整交互待验收 |
| R13 | 日期 epoch/星期转换、下一次计划 | W3；已验证 |
| R14 | 跨午夜星期归属 | W3；静态 |
| R15 | 启动/恢复漏过时间不补执行 | W3；静态 |
| R16 | 进程基线、连续缺席宽限 | W3；静态 |
| R17 | 旧 occurrence 兼容、有界记账 | W2/W3；静态 |
| R18 | 单条坏规则隔离、保留原文 | W2；静态 |
| R19 | 同名不同路径实例、扫描失败语义 | W3；静态 |
| R20 | 太阳时角重复除二 | W5；已验证 |
| R21 | 手动主题覆盖跨日到期 | W5；静态 |
| R22 | 系统观察状态、目标、应用结果分离 | W5；静态 |
| R23 | 定位回退、缓存失效、极昼夜回退 | W5；静态 |
| R24 | IP 退避、超时、UI 不同步联网 | W5；静态 |
| R25 | HTTP/坐标/响应分配限制 | W5；静态 |
| R26 | GPU 预热、有效采样、PID 字段匹配 | W1；静态 |
| R27 | 副屏全屏及演示模式检测 | W5；静态/实机 |
| R28 | Windows 版本读取位置与能力判断 | W5；静态 |
| R29 | 辅助主题实际应用、色值恢复、COM 等价性 | W5；静态/实机 |
| R30 | uxtheme ordinal 版本分流与最低系统 | W5；静态/实机 |
| R31 | 托盘/IPC/第二实例聚焦当前模态窗 | W4；静态 |
| R32 | Tab/Enter/Esc、星期键盘导航 | W4；静态 |
| R33 | choice 系统操作提交分派 | W4；静态 |
| R34 | 过滤不丢隐藏选择 | W4；静态 |
| R35 | 路径可撤销，按规则决定零选择 | W4；静态 |
| R36 | 创建进程摘要并回传说明 | W4；静态 |
| R37 | 异步加载阶段、代次、滚动/焦点保持 | W4；静态 |
| R38 | 按 ID 保持选择和草稿冲突检测 | W2/W4；静态 |
| R39 | 各窗 DPI、viewport、草稿/焦点保留 | W4；静态/实机 |
| R40 | 主题重建列表/checkbox 资源 | W4；静态 |
| R41 | 子类原过程、HWND registry 生命周期 | W4；静态 |
| R42 | 语言热切换、CLI 翻译初始化 | W6；静态 |
| R43 | 多占位符顺序替换 | W6；静态 |
| R44 | 所有状态展示和 tooltip 读有效状态 | W6/W4；静态 |
| R45 | 配置/UI 数值边界及窄化转换 | W2；静态 |
| R46 | last-known-good、模板、注释、热加载 | W2；静态 |
| R47 | 管道连接竞态、响应交付、非幂等重试 | W6；静态 |
| R48 | CLI 参数、错误前缀/退出码、reload | W6；静态 |
| R49 | 系统动作/权限/能力检测返回结果 | W3/W6；静态 |
| R50 | 增强输入计数及独立有效空闲时钟 | W3；静态 |
| R51 | 告警文案/来源/取消及多行布局 | W3/W4；静态 |
| R52 | 自绘可访问状态、文本缩放/高对比 | W4；静态/实机 |
| R53 | 日志开关/轮转/会话/trace | W6；静态 |
| R54 | devtools 秒数与持久配置隔离 | W0；静态 |
| R55 | icon/font/library/DC 等资源生命周期 | W1/W4/W6；静态 |
| R56 | 托盘/长路径失败的可访问入口 | W6；静态 |

额外独立工作：主题拓扑协调与修复冷却（W5）；原列表列宽、tooltips、表头/滚动条（W4）；开发/用户文档与发布验证（W6）。未决策的行为按整体方案集中确认，不在重构中静默改动。

## 原 UI 清单与工作包映射

| 原项目 | 对应位置 |
|---|---|
| 空白 Bug | R12 / W4，根因与真实交互验收 |
| 差距 1 说明回传 | R36 / W4 |
| 差距 2 加载分相位和激活刷新 | R37 / W4 |
| 差距 3 管理器选择 | R38 / W2、W4 |
| 差距 4 保存失败 | R01 / W2，保留草稿/窗口 |
| 差距 5 星期键盘 | R32 / W4 |
| 差距 6 列宽 | W4 独立验收项 |
| 差距 7 滚动/焦点 | R37 / W4 |
| 差距 8 choice 完整语义 | R33/R41 / W4，保留原全部子项 |
| 差距 9 checkbox 位图 | R40 / W4 |
| 差距 10 外部冲突 | R38 / W2、W4 |
| tooltips、滚动/DPI、表头/滚动条 | W4 独立完成条件 |
| 文档恢复 | W6 |
| Cargo.lock | 已完成 |
